{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "Scaffold-Controller",
  "title": "",
  "type": "object",
  "properties": {
    "prismaModel": {
      "type": "string",
      "description": "The name of the prisma model in the schema.prisma file.",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "project": {
      "type": "string",
      "description": "The project where the generated files are placed.",
      "$default": {
        "$source": "projectName"
      },
      "alias": "p",
      "x-prompt": "What project would you like to target?"
    },
    "domainProject": {
      "type": "string",
      "description": "The domain project to import interfaces from (e.g. IRepo, CreateDto, etc.).",
      "$default": {
        "$source": "projectName"
      },
      "alias": "p",
      "x-prompt": "From which domain project to import interfaces from?"
    },
    "infrastructureProject": {
      "type": "string",
      "description": "The infrastructure project to import the repository or service.",
      "$default": {
        "$source": "projectName"
      },
      "alias": "p",
      "x-prompt": "From which domain project to import interfaces from?"
    },
    "directory": {
      "type": "string",
      "description": "The directory where the generated files are placed.",
      "aliases": ["dir", "path"]
    },
    "idType": {
      "type": "string",
      "description": "The type of the id field.",
      "default": "number",
      "enum": ["number", "string"]
    },
    "skipFormat": {
      "type": "boolean",
      "description": "Skip formatting files.",
      "default": false
    }
  },
  "additionalProperties": false,
  "required": [
    "prismaModel",
    "project",
    "domainProject",
    "infrastructureProject"
  ]
}
